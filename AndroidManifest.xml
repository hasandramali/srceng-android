<?xml version="1.0" encoding="utf-8"?>
<manifest
    android:installLocation="auto"
    package="com.valvesoftware.source"
    platformBuildVersionCode="29"
    platformBuildVersionName="10"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Uygulamaya ağ yuvaları oluşturma ve özel ağ protokolleri kullanma izni verir. Tarayıcı ve diğer uygulamalar İnternet'e veri gönderilmesi için araç sağlarlar, bu nedenle bu izin, İnternet'e veri göndermek için gerekli değildir. -->
    <uses-permission
        android:name="android.permission.INTERNET" />
    <!-- Uygulamaya, telefonun uykuya geçmesini önleme izni verir. -->
    <uses-permission
        android:name="android.permission.WAKE_LOCK" />
    <!-- Uygulamaya, hangi ağların bulunduğu ve hangilerinin bağlı olduğu gibi ağ bağlantılarıyla ilgili bilgileri görüntüleme izni verir. -->
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Uygulamaya, Kablosuz bağlantının etkin olup olmadığı ve bağlanan Kablosuz cihazların adları gibi Kablosuz ağ kullanımıyla ilgili bilgileri görüntüleme izni verir. -->
    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Uygulamanın paylaşılan depolama alanınıza içerik yazmasına izin verir. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Bu uygulama, kullanıldığı sırada mikrofonu kullanarak ses kaydedebilir. -->
    <uses-permission
        android:name="android.permission.RECORD_AUDIO" />
    <uses-feature
        android:name="android.hardware.screen.landscape"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00020000" />
    <application
        android:label="@string/srceng_app_name"
        android:debuggable="true"
        android:allowAudioPlaybackCapture="true"
        android:requestLegacyExternalStorage="true">
        <activity
            android:label="@string/srceng_app_name"
            android:name="me.nillerusr.LauncherActivity"
            android:windowSoftInputMode="adjustResize|stateHidden">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />
                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="Directory"
            android:name="me.nillerusr.DirchActivity"
            android:windowSoftInputMode="adjustResize|stateHidden">
            <intent-filter>
                <action
                    android:name="android.intent.action.DIRCH" />
                <category
                    android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:label="@string/srceng_app_name"
            android:name="org.libsdl.app.SDLActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|layoutDirection|locale|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode">
            <intent-filter>
                <action
                    android:name="org.libsdl.app.SDLACTIVITY" />
                <category
                    android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <service
            android:name="me.nillerusr.UpdateService"
            android:stopWithTask="false" />
        <meta-data
            android:name="android.max_aspect"
            android:value="9999.0" />
        <meta-data
            android:name="android.allow_multiple_resumed_activities"
            android:value="true" />
    </application>
</manifest>
